apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "formbuilder.fullname" . }}-test-connection"
  labels:
    {{- include "formbuilder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "formbuilder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
